apiVersion: v1
kind: Service
metadata:
  name: media-server
  labels:
    app: media-server
spec:
  type: ClusterIP
  selector:
    app: media-server
  ports:
    - name: http
      port: 8095
      targetPort: 8095
