apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-registry-service
  labels:
    app: app-registry-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-registry-service
  template:
    metadata:
      labels:
        app: app-registry-service
    spec:
      containers:
        - name: app-registry-service
          imagePullPolicy: IfNotPresent
          image: nats-chat/app-registry-service:latest
          env:
            - name: NATS_URL
              value: nats://nats:4222
            - name: NATS_USER
              value: app-registry-service
            - name: NATS_PASS
              value: app-registry-service-secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DATABASE_URL
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4317
            - name: OTEL_SERVICE_NAME
              value: app-registry-service
